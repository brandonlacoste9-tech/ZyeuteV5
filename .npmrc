# Fix for npm bug #4828 - Railway deployment specific
optional=true
save-optional=true

# Force installation in Railway (Linux) environment
# Note: This file ensures optional dependencies are installed correctly

# Railway deployment optimizations
audit=false
fund=false
progress=false

# Ensure deterministic builds
package-lock=true